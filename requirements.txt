fastapi>=0.135.3
uvicorn[standard]>=0.44.0
pydantic[email]>=2.13.0
pydantic-settings>=2.8.0
sqlalchemy[asyncio]>=2.0.49
asyncpg>=0.31.0
alembic>=1.18.4
redis>=6.2.0
celery[redis]>=5.6.3
structlog>=25.5.0
httpx[socks]>=0.28.1

# Ingestion: RSS parsing plus browser-impersonating fetch and article
# extraction. Playwright, crawl4ai and the PDF stack went with the government
# portal crawlers, which were the only things that needed them.
curl_cffi>=0.15.0
trafilatura>=2.0.0
feedparser>=6.0.10
beautifulsoup4>=4.14.3
lingua-language-detector>=2.1.1

litellm>=1.83.0
ddgs>=9.14.4
croniter>=6.2.2
